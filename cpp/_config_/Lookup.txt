
#include <string>

enum class #ENTITY_NAME_PRETTY#
{
#ENUM_DECLARATION#
};

class #ENTITY_NAME_PRETTY#Converter
{
public:
#ENUM_MEMBERS#

    static #ENTITY_NAME_PRETTY# FromToken( std::string token ) {
#ENUM_FROM_TOKEN#

        throw std::string( "invalid token '" ) + token + std::string( "'" );
    }

    static std::string ToToken( #ENTITY_NAME_PRETTY# id ) {
#ENUM_TO_TOKEN#

        throw std::string( "invalid id '" ) + std::to_string( static_cast<int32_t>( id ) ) + std::string( "'" );
    }
};
